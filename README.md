## ✨ ১. TypeScript‑এ **interface** এবং **type** এর মধ্যে কী কী পার্থক্য আছে?

TypeScript‑এ ডেটা স্ট্রাকচার নির্ধারণ করার দুটি জনপ্রিয় উপায় হলো **interface** এবং **type alias**। অনেক সময় তারা একই রকম কাজ করলেও কিছু মূল পার্থক্য রয়েছে।

### 🔹 **১. Declaration Merging**

* **Interface:** একাধিকবার ঘোষণা করলে সেগুলো merge হয়।
* **Type:** Duplicate declaration করলে error দেয়, merge হয় না।

### 🔹 **২. Extending / Inheritance**

* **Interface:** অন্য interface বা type সহজেই `extends` করতে পারে।
* **Type:** `&` (intersection) ব্যবহার করে combine করতে হয়।

### 🔹 **৩. ব্যবহার ক্ষেত্র**

* **Interface:** object structure এবং class contract তৈরিতে বেশি উপযোগী।
* **Type:** অনেক বেশি flexible—union, intersection, primitive, tuple ইত্যাদি represent করতে পারে।

### 🔹 **৪. জটিল টাইপ সাপোর্ট**

* **Interface:** union টাইপ define করতে পারে না।
* **Type:** union, intersection—সব করতে পারে।


## ✨ ২. TypeScript‑এ **keyof** কীওয়ার্ডের কাজ কী? (উদাহরণসহ)

`keyof` একটি টাইপের **সবগুলো key‑এর union** তৈরি করে।

### 📘 উদ্দেশ্য

`keyof` ব্যবহারের মাধ্যমে:

* Object‑এর key গুলো টাইপ‑সেইফভাবে অ্যাক্সেস করা যায়।
* Dynamic ভাবে key‑ভিত্তিক ফাংশন তৈরি করা যায়।

### 📌 উদাহরণ

```ts
interface User {
  name: string;
  age: number;
  email: string;
}


type UserKeys = keyof User;

function getValue(obj: User, key: UserKeys) {
  return obj[key];
}

const user: User = {
  name: "Habib",
  age: 20,
  email: "habib@example.com"
};

console.log(getValue(user, "name"));
```

`keyof` ব্যবহার করলে আপনি শুধুমাত্র বৈধ key অ্যাক্সেস করতে পারবেন, যা টাইপ সেফটি বাড়ায় এবং bug কমায়।
